
    """def help(self):
        result = []
        return self.help_list()

        for command in self.help_list():
            row = ('%s — %s' % (command[0], command[1]))
            result.append(row)

        return 'n'.join(result)"""